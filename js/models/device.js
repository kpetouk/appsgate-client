define([
	"underscore",
	"backbone",
	"models/homecomponent"
], function(_, Backbone, HomeComponentModel) {

	/**
	 * @class DeviceModel
	 */
	var DeviceModel = HomeComponentModel.extend({
	});

	return DeviceModel;

});
